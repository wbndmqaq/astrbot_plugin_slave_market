## 1.0.1 (2026-09-13)

### 📝 文案体系（全部外置）

- **新增 `uiTexts.json`（221 键）**：此前约 150 条交互回复散落在 svc 层代码里、
  10 个模板的全部标题/字段名写死在 HTML 中，WebUI 文案编辑器都改不到。现在
  全部指令回复（购买/抢劫/训练/决斗/排位/银行/备份/打工/榜单）与模板静态文案
  统一走 `ui_text(key, default)` / 模板 `{{ t.键 }}`，WebUI「文案」页可在线编辑并热更新
- 键名带 `{占位符}` 的文案由宽容填充：缺变量/多写占位原样显示，改坏文案不会打断指令；
  文案编辑器支持 uiTexts 的在线编辑（字符串键单行输入、留空拒绝保存）
- 代码内置 default 与 JSON 值由 lint 测试逐键核对漂移；模板正文不再出现 `{{ t. }}` 之外的中文
- 新建 tests/（40 项，含密码哈希链路与文案编辑器入口守护）：文案三方一致性 lint、历史缺陷回归（段位一致/幽灵建档/身价守恒/事务回滚）、
  29 条路由装配与 10 套模板渲染冒烟、配置键零死键扫描
- 签入 ruff.toml 并清理全部 lint 告警（与上班族物语同一套规则）

### 🔐 安全

- **管理员密码明文不再落盘**：面板改密 / 强制改密 / 配置面板改密三条路径此前都会把
  新密码**明文**写回插件配置（与「明文不出现在磁盘」的安全声明直接矛盾）。现在改密
  后回写的是 Argon2id **哈希串**，启动引导按 `$argon2` 前缀识别哈希形态并直接装入
  密码存储——「配置项生效」的语义完全保留，全程不经手明文；按配置明文重置密码成功
  后也会立即把配置替换为哈希形态。配置面板不再回显哈希值，也不接受把哈希串当新密码
  提交（防呆提示）。

### 🛠 修复

- **新号段位与段位说明自相矛盾**：新号此前是「1000 分 / 青铜」，而段位判定与说明把
  门槛当上界（青铜 <1000），第一场排位后会被静默改成白银。现在新号初始分取首档门槛
  −1（默认 999/青铜），与段位说明始终一致
- **赎身/购买可能给已被删档的主人凭空建档送钱**（幽灵玩家）：事务内先校验对方存档
  仍在，已注销时赎身金不再支付、购买价款改按无主处理归奴隶本人
- **决斗身价守恒在触底时失效**：败者跌幅被身价下限钳小后，胜者仍按未钳制的跌幅上涨，
  每次触底决斗都在净通涨；现在胜者涨幅按败者【实际跌幅】结算
- WebUI 玩家编辑的存在性校验移入事务内：校验与写入之间目标被删档时，不再用面板表单
  值把档案复活成幽灵行
- WebUI 帮助文案编辑器：混在有效条目里的空行不再被静默删除后再保存（与后端校验同口径，
  保存前明确提示）

### 📝 文档

- CHANGELOG：决斗「出手/伤害/克制文案池」改为实际存在的动作文案池；「可撤销任意设备
  会话」改为实际能力（登出吊销当前会话 / 改密下线全部会话）；打工与一键打工合并说明；
  删除不存在的「下拉选项」描述
- README：购买结算说明与实际一致；指令表与 metadata 补上「购买群友」别名
- WebUI 快速上手不再写死与配置联动的具体数值（+20 身价 / 10% 摸鱼）

## 1.0.0 (2026-09-12)

> **奴隶市场** —— 群内互动经营游戏，首个正式版本（由 Yunzai 插件 Slave-Market 移植）。
>
> 购买群友当奴隶、让奴隶打工赚钱、训练 / 决斗 / 排位赛提升奴隶身价、银行存取款吃利息、
> 抢劫与赎身；**29 条指令**、**20 项在线可调配置**、独立端口的 WebUI 管理面板，
> 全部输出以 HTML 卡片出图；文案与数值全部外置 JSON，可在面板里在线编辑并热更新。

### 🛒 奴隶交易

- **购买奴隶**：@ 群友或直接给 QQ 号即可购买（`购买奴隶` / `购买群友` 均可触发），购买价随对方身价浮动，成交后其身价上涨；有原主时购买价归原主人，无主时卖身钱归奴隶本人
- **我的奴隶**：一次列出全部奴隶的身价、主人与状态，免去逐个查询
- **放生 / 赎身**：赎身按赎身价计税，支持每周次数上限与冷却限制
- **奴隶市场**：本群全体可购买玩家一览（含身价与当前主人）

### 💼 打工与养成

- **打工 / 一键打工**：同一条指令（`#打工` 与 `#一键打工` 等价）：没有奴隶时自己打工，有奴隶时指挥全部奴隶一次打完；收入按身价结算，结果从文案池随机抽取（奴隶主与奴隶各有专属文案）
- **训练**：单个训练与一键训练，花费 / 成功率 / 身价涨幅均可配，成功提升身价
- **决斗**：指定双方奴隶出战，动作文案池可在面板在线编辑
- **排位赛**：段位制晋级，对手名单、事件增益、段位阈值与顶级段位名称全部可配；
  可查看本人的排位赛信息与段位说明

### 🏦 银行与财富

- **存取款**：存款 / 取款 / 一键存款，存储上限随信用等级提升
- **利息**：按小时结算，最多累积 24 小时，可领取
- **信用等级**：升级信用 / 一键升级信用，价格严格递增、支持批量连升
- **转账**：含手续费与最低金额限制
- **抢劫**：成功率、罚金比例、单次封顶全部可配，失败会被罚款

### 🏆 排行与查询

- **四大排行榜**：奴隶身价榜、奴隶资金榜、金币榜、银行等级榜
- **个人资料**：金币、身价、主人、奴隶数、段位与战绩一屏可查

### 🛠 管理与数据安全

- **SQLite 单文件存档**，WAL 模式，所有改钱操作走单事务，避免并发下重复结算
- **备份系列**（管理员）：创建快照 / 备份列表 / 恢复备份 / 删除备份，
  恢复前自动留档一份当前库，防止恢复错了没有回头路
- **坏行取证**：解析失败的玩家行进回收站并打标，不阻塞其余玩家
- **文案改动不写插件目录**：落到插件数据目录并按「用户覆盖 > 内置默认」合并，
  插件升级不会吞掉编辑成果，`resources/` 只读挂载也能保存

### 🖥 独立 WebUI 管理面板

- **独立端口**（默认 `127.0.0.1:17818`，可改监听地址与端口），不占用 AstrBot 主面板
- **认证**：Argon2id 口令哈希 + JWT(HS256) + 服务端会话表，退出登录吊销当前会话；
  改密立即下线全部会话
- **总览**：玩家 / 群 / 金币 / 银行 / 奴隶数量统计
- **玩家管理**：按群分页列表、跨群搜索、逐玩家数值编辑
- **备份管理**：创建 / 列表 / 恢复 / 删除
- **配置面板**：全部配置项在线调整，数值项带 min/max 范围校验
- **文案在线编辑并热更新**：打工文案、决斗 / 排位赛文案、帮助长文本
- **安全默认**：非本机监听要求显式设置过密码；Host / Origin / CSRF 校验、
  登录限速与全站失败闸门、请求体上限、密码与密钥永不回显

### 🎨 表现与扩展

- 全部输出走 **Playwright + Jinja2 模板出图**（10 套卡片：竞技场 / 银行 / 帮助 /
  市场 / 我的奴隶 / 通知 / 排位赛 / 排行榜 / 训练 / 打工），
  渲染环境缺失时自动回退纯文本并提示安装步骤，绝不中断指令
- **文案与数值全外置 JSON**，改名 / 改文案 / 调数值都不用改代码
- **20 项在线可调配置**：购买冷却、打工收益、抢劫成功率、训练花费、段位与银行参数等
- 指令前缀 `！` / `!` 均可，别名丰富（`群友` / `nl` 系列、`打劫`、`一键工作` 等）
